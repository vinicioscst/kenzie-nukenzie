import styled from "styled-components";

export const StyledAside = styled.aside`
  display: flex;
  flex-direction: column;
  gap: 1.25rem;

  min-width: clamp(225px, 100%, 355px);
  flex-grow: 1;
`;
